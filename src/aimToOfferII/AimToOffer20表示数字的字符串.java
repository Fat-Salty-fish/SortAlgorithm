package aimToOfferII;

/**
 * @author lizhongjie
 * @desc
 * @create_time 2022/3/30
 */
public class AimToOffer20表示数字的字符串 {

    /**
     * 判断一个字符串是否能解析为数字
     * 怎么感觉还是动态规划 但又不太像 不太行 动态规划
     *
     * @param s
     * @return
     */
    public boolean isNumber(String s) {
        if (s == null || s.length() == 0) {
            return false;
        }
        // todo
        return false;
    }
}
